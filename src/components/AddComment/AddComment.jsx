const AddComment = () => {
  return ( 
    <>
      <button>Add Comment</button>
      {/* <button onClick={() => handleComment(post._id)}>&times;</button> */}
   
    </>

   );
}
 
export default AddComment;